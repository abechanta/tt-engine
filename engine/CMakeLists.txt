set(TARGET_NAME ttengine)
add_library(${TARGET_NAME} STATIC tte_main.cpp)
include_directories(${Boost_INCLUDE_DIRS} ${SDL2_INCLUDE_DIRS} .)
target_compile_features(${TARGET_NAME} PRIVATE cxx_std_17)
target_link_libraries(${TARGET_NAME} PRIVATE ${SDL2_LIBRARY} SDL2::SDL2_image)
